<form method="post" action="{{ path('app_rdv_delete', {'idRdv': rdv.idRdv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rdv.idRdv) }}">
    <button class="btn">Delete</button>
</form>